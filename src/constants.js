const extraExcludeDirectory = ['__mocks__', '__tests__', '__snapshots__', 'node_modules'];

const requiredPluginOptions = ['loggerMethod', 'loggerName'];

const windowObject = 'window.';

export { extraExcludeDirectory, requiredPluginOptions, windowObject };
